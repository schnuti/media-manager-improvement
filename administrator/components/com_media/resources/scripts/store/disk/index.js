// The initial state
const state = {
    // A list of directories loaded by the client
    directories: [
        {path: '/', name: 'PLACEHOLDER', directories: [], files: [], directory: null}
    ],
    // A list of files
    files: [],
};

// Getters
const getters = {};

// Actions
const actions = {
};

// Mutations
const mutations = {

};

export default {
    namespaced: true,
    state,
    getters,
    actions,
    mutations
};